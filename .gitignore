# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# --- Build outputs (dist/ is generated by scripts/build.py) ---
# dist/ is intentionally NOT ignored so consumers can download the
# ready-to-use merged files without running the build.
# vehicle-names-database/dist/*.tmp

# --- Editors / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# --- Design assets (local only, not part of the data repo) ---
github-intro-cover/

# --- One-off working scripts / audit outputs (not part of the repo) ---
.tmp_*
rename_logos.py
reprocess_logos.py

# --- Obsidian workspace files ---
.obsidian/
